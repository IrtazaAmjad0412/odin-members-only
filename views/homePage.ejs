<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="nav">
      <h1><%= title %></h1>
      <%- include("navbar.ejs") %>
    </div>
    <div class="post-list">
      <% posts.forEach((post)=> { %>
      <div class="post-card"></div>
      <% }) %>
    </div>
  </body>
</html>
